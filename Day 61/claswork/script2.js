let isSunny = true // if the day is sunny
let isRaining = false // if it is raining

if (isSunny && !isRaining) {
  console.log("You can go outside to play!")
} else {
  console.log("You cannot go outside to play")
}
