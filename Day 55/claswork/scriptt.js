const firstName = prompt("Enter your first name:");
const lastName = prompt("Enter your last name:");
const age = prompt("Enter your age:");
const location = prompt("Enter your place of residence:");

const message = `User ${firstName} ${lastName} is ${age} years old and lives in ${location}.`;

console.log(message);
alert(message);
